{
  "configuration": {
    "title": "AWS STS",
    "type": "object",
    "properties": {
      "aws_role_arn": {
        "description": "The ARN of the AWS role to assume",
        "type": "string"
      },
      "aws_access_key": {
        "description": "The identifier of the access key",
        "type": "string"
      },
      "aws_secret_access_key": {
        "description": "The secret associated to the access key",
        "type": "string"
      },
      "aws_region_name": {
        "description": "The area hosting the AWS resources",
        "type": "string"
      }
    },
    "required": [
      "aws_region_name"
    ],
    "secrets": [
      "aws_secret_access_key",
      "api_key"
    ]
  },
  "description": "AWS STS (Security Token Service) is a web service that enables you to request temporary, limited-privilege credentials for AWS Identity and Access Management (IAM) users or for users that you authenticate (federated users). This module provides triggers to collect events from AWS STS",
  "name": "AWS STS",
  "uuid": "1490ee64-8a9a-4961-87c6-621f8cd67546",
  "slug": "aws-sts",
  "version": "1.35.0",
  "categories": [
    "Cloud Providers"
  ],
  "supports_validation": true
}
